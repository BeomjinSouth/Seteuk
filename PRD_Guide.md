## 제품 요구 사항 문서(PRD) 초안: 수행평가 분석 파이프라인 개선

### 0. 한 줄 요약

수행평가 파일(PDF/이미지)을 업로드하면, **공통 자료(조건 카드/상황 글/지시문/표/그림)를 먼저 분리·연결**하고, **문항 유형(개방형/증명형/정리형 등)에 맞는 출력(루브릭·예시답안 중심)**을 생성하여 **헛지적(불명확/논쟁 등)을 최소화**한다.

---

## 1) 배경과 문제 정의

### 1.1 현재 관찰된 문제(이번 PDF에서 특히 잘 드러남)

* 수행평가 활동지는 정답이 하나로 고정되지 않는 문항이 많다.
* 공통 자료(조건 카드 등)가 문항 본문 밖(상단/별도 박스)에 있어, 문항만 따로 떼어 분석하면 “해당 조건” 같은 표현이 갑자기 **무엇을 가리키는지 사라진다**. 이번 PDF도 조건 카드 1~3이 문항 상단 공통 자료로 제시되고 , 문항에서는 “선택한 <조건 카드>”, “해당 조건” 같은 지시 표현이 반복됩니다. 
* 그 결과 모델이 “조건이 불명확하다”, “구체성이 부족하다” 같은 **헛지적**을 만들어 내기 쉬워진다.

### 1.2 목표 문제(근본 원인)

* 파이프라인이 문서를 “정답형 시험지”로 전제하고 있어,

  * (1) 공통 자료를 문서 레벨에서 잡아내지 못하고
  * (2) 문항별 분석에서 정답·풀이·수정안을 강제하며
  * (3) 수행평가의 “열린 지시”를 결함으로 오판하는 문제가 반복된다.

---

## 2) 목표(Goals)와 비목표(Non-goals)

### 2.1 목표(반드시 달성)

1. **PDF를 이미지로 변환한 멀티모달 인식**을 기본으로 하되,
   **PDF 텍스트 추출 결과와 비교·대조하여 품질을 안정화**한다.
2. 공통 자료(조건 카드, 공통 지문, 상황 글, 활동 지시문, 표/그림)를 **sharedResources로 1급 객체화**하고, 각 문항이 **resourceRefs로 참조**하도록 한다.
3. 문항을 “정답형/개방형/증명형/정리형/조건선택-분기형…”으로 **문항 유형 분류(taskType)** 하고,
   그 유형에 맞는 **출력 스키마(answerType)와 프롬프트를 분기**한다.
4. **이슈(문제점) 남발을 방지**한다.
   수행평가에서 의도된 개방성(예: “나의 예측”)은 원칙적으로 결함이 아니다.
5. 저장소는 **구글 스프레드시트 + 구글 드라이브만** 사용한다(긴 내용 저장 가능).

### 2.2 비목표(이번 범위에서 하지 않음)

* 학생 답안을 실제로 자동 채점해 점수를 내는 기능
* 교육과정 밖 지식까지 동원한 고난도 정리/증명 자동 작성(다만 “증명 개요” 수준은 포함)

---

## 3) 사용자 요구(당신 조건 반영)

### 3.1 입력 처리 요구

* PDF 입력:

  1. 페이지별 이미지로 변환(필수)
  2. 동시에 PDF에서 텍스트도 추출(가능하면)
  3. 멀티모달 인식 결과(이미지 기반 텍스트/구조)와 PDF 텍스트를 비교해, 누락/오인식을 줄인다.
* 이미지 입력: 그대로 사용.

### 3.2 출력 언어 및 교과 특색

* 기본 출력: **한국어 고정**
* 다만 영어 교과 등에서 지문/선지 자체가 영어면:

  * “원문은 원문 그대로 보존”
  * “설명/루브릭/지도 포인트는 한국어”
  * 필요할 때만 영어 문장을 그대로 인용(원문 보존 목적)

### 3.3 저장

* 스프레드시트: 긴 텍스트/제이슨 문자열 저장 허용
* 드라이브: 분석 결과 원문(매니페스트 제이슨) 저장 가능

---

## 4) 핵심 설계 원칙(이 PRD의 제일 중요한 부분)

### 4.1 “공통 자료 우선” 원칙

수행평가에서 문항의 의미는 “문항 본문”만으로 완성되지 않는 경우가 많으므로,

* 먼저 공통 자료(조건 카드/상황 글/지시문/표/그림)를 분리해 저장하고,
* 각 문항 분석 시 반드시 참조 자료를 함께 넣는다.

이번 PDF에서 조건 카드가 공통 자료이고 , 1번 문항 지시가 그 공통 자료에 의존한다는 점 이 대표 예시입니다.

### 4.2 “정답 강제 금지” 원칙(수행평가/개방형)

개방형 문항에서는 정답을 단정하기보다 다음을 출력해야 한다.

* 예시 답안 2~3개
* 채점 요소(루브릭) 3개 이상
* 학생 오해 포인트
* 교사용 지도 포인트

### 4.3 “이슈는 진짜 결함일 때만” 원칙

* 수행평가 특성(열린 지시어, 다양한 답안)을 문제점으로 찍지 않는다.
* 이슈가 없으면 `issues = []`가 정상이다.
* 수정 제안은 “문제점이 있을 때만” 생성한다.

---

## 5) 기능 요구 사항(Functional Requirements)

### 5.1 전처리 단계

**FR-1: PDF → 페이지 이미지 변환(필수)**

* 입력: PDF 파일
* 출력: `pageImages[]` (각 원소는 `data:image/png;base64,...`)
* 품질 기준: 글자가 흐릿하지 않게 충분한 해상도(예: 150~300 DPI 수준 정책)

**FR-2: PDF 텍스트 추출(가능하면)**

* 입력: 같은 PDF
* 출력: `pdfText` (페이지별 텍스트를 합치거나 배열로 유지)
* 목적: 멀티모달 인식 결과와 비교하여 누락/오인식을 줄이는 “정합성 체크”에 사용

**FR-3: 이미지 기반 문서 인식(멀티모달)**

* 입력: `pageImages[]`
* 출력: 구조화에 필요한 시각 정보(박스, 표, 카드, 문항 번호 등)

**FR-4: 정합성 비교(텍스트 대조)**

* `visionExtractText`(이미지 기반 추출) vs `pdfText` 비교
* 비교 결과로 다음을 생성:
* `consistencyReport`(누락 가능 영역, 번호 누락 가능, 카드 텍스트 차이 등)
* 이 리포트는 구조화 프롬프트에 “참고 정보”로 제공한다(즉시 수정을 강제하지는 않음).

---

### 5.2 구조화 단계(문항/공통자료 추출)

**FR-5: sharedResources 추출**

* 조건 카드, 상황 글, 활동 지시문, 공통 지문, 표/그림, 참고 박스 등
* 리소스는 아래 필드를 가진다.

  * `resourceId`(고유)
  * `type`(예: condition_cards, scenario, instructions, table, image, passage)
  * `title`
  * `content`(원문 텍스트)
  * `items`(카드/선택지 묶음인 경우: label+content 배열)

**FR-6: questions 추출 + resourceRefs 연결**

* 문항별 필드:

  * `questionNumber`, `displayName`, `pageRange`
  * `bodyText`
  * `taskType`(문항 유형)
  * `resourceRefs`(참조하는 공통 자료 ID 배열)
  * `hasImage`, `imageDescription`(필요 시)

**FR-7: 문항 유형(taskType) 분류 기준(예시)**

* `open_ended`(예측/서술/작성)
* `proof`(증명)
* `drawing`(작도/그리기)
* `reflection`(활동 정리/성찰)
* `conditional`(조건 카드 선택으로 분기되는 구조)
* `single_answer`, `multiple_answer`(정답형)

이번 PDF는 조건 카드 선택이 핵심이고 , 3번은 “성립 증명” , 4번은 “활동 정리”  유형입니다.

---

### 5.3 분석 단계(문항 유형별 출력)

**FR-8: 분석 출력은 answerType 기반**

* `answerType` 값:

  * `single`
  * `multiple`
  * `open_ended`
  * `conditional`

**FR-9: open_ended일 때 필수 출력**

* `rubric[]`(채점 요소)
* `sampleResponses[]`(예시 답안)
* `teacherNotes`(지도 포인트)
* `commonMisconceptions`(학생 오해 포인트)

**FR-10: proof일 때 필수 출력**

* `proofOutline`(핵심 단계)
* `requiredProperties`(사용 성질 목록)
* `teacherNotes`

**FR-11: conditional(조건 카드 선택형)일 때 필수 출력**

* `conditionalOutcomes[]`

  * 각 카드(label)별로:

    * `predictedConclusion`
    * `proofOutline` 또는 `reasoningGuide`
    * `rubric`(가능하면 공통/카드별)

**FR-12: issues 정책**

* 실제 결함일 때만 기록
* “참조 리소스가 존재하는 지시어”는 원칙적으로 문제점으로 잡지 않음
* 이슈가 없으면 `issues=[]`
* `suggestion`은 이슈가 있을 때만 생성(옵션)

---

## 6) 데이터 스키마(제이슨 스키마 변경 요약)

### 6.1 구조화 스키마 변경(핵심)

* 최상단에 `sharedResources` 추가
* 각 question에 `taskType`, `resourceRefs` 추가

### 6.2 분석 스키마 변경(핵심)

* `answerType` 추가
* open_ended/proof/conditional에 맞는 필드 추가
* `suggestion` required 제거(옵션화)

---

## 7) 저장 설계(스프레드시트 + 드라이브)

### 7.1 스프레드시트 시트(탭) 구성 권장

기존(문서/문항/이슈) 구조를 유지하면서 아래 중 하나를 선택.

**안 A: 단순(추천) — 한 셀에 제이슨 저장**

* `EvalCheckDocuments`
* `EvalCheckQuestions`

  * 새 컬럼: `taskType`, `answerType`, `resourceRefsJson`, `analysisJson`
* `EvalCheckIssues`
* `EvalCheckResources`

  * `sharedResourcesJson`을 한 행 또는 리소스별 여러 행으로 저장

**안 B: 정규화 — 연결 테이블 분리**

* `EvalCheckQuestionResourceRefs` 시트 추가(문항ID-리소스ID 매핑)

당신이 “긴 내용 저장 괜찮다”고 했으므로, 운영 편의상 **안 A가 초기에는 가장 빠르고 안정적**입니다.

### 7.2 드라이브 저장

* 문서별 `manifest.json` 저장(구조화 결과 + 리소스 + 분석 결과 전체)
* 스프레드시트에는 manifest 파일 ID를 저장해 “원문 전체”를 언제든 복원 가능

---

## 8) 품질 기준과 자동 검증(테스트)

### 8.1 필수 품질 지표

* 공통 자료 추출 성공률(조건 카드/상황 글/지시문을 sharedResources로 잡는 비율)
* 문항 유형 분류 안정성(같은 문서 반복 분석 시 taskType이 크게 흔들리지 않는가)
* “헛지적” 비율(교사가 봤을 때 불필요한 misunderstanding/controversy 감소)

### 8.2 자동 검증 규칙(예시)

* 문항 본문에 “해당 조건/선택한 카드”가 있는데 `resourceRefs`가 비어 있으면 경고
* conditional 문항인데 `conditionalOutcomes`가 비어 있으면 재시도 또는 품질 경고
* open_ended 문항인데 `rubric`이 3개 미만이면 재시도 또는 경고

---

## 9) 파이프라인(수정 후 목표 흐름)

1. 업로드
2. PDF면:

   * 페이지 이미지 변환
   * PDF 텍스트 추출
   * (가능하면) 정합성 비교 리포트 생성
3. 구조화 호출(멀티모달):

   * sharedResources + questions + resourceRefs + taskType 생성
4. 문서 컨텍스트 생성(요약):

   * sharedResources 우선 보존, 나머지 요약
5. 문항별 분석(유형 분기):

   * question + referenced resources + subject + document context
6. 저장(시트 + 드라이브 manifest)

---

## 10) 출력 언어 정책(한국어 고정 + 교과 특색)

* 모든 “설명/루브릭/지도 포인트”는 한국어
* 영어 과목의 경우:

  * 지문/선지/원문은 원문 그대로 포함(영어 유지)
  * 해설은 한국어
* 수학 과목의 경우:

  * “증명 개요”, “사용 성질”, “반례 가능성” 같은 지도 요소를 우선 출력

---

## 11) 구현 범위(코드 전면 수정 시 PRD가 요구하는 변경점 요약)

### 11.1 반드시 바꿔야 하는 코드 포인트

* PDF 입력을 `data:application/pdf;base64,...` 형태로 멀티모달에 직접 넣지 않도록:
  **“항상 페이지 이미지 배열”로 변환한 뒤** `image_url`로 넣기
* `extractQuestionStructure`:

  * 스키마 확장(sharedResources, resourceRefs, taskType)
  * 프롬프트 수정(공통 자료 추출/연결 강제)
* `analyzeQuestion`:

  * 스키마 확장(answerType, rubric 등)
  * 프롬프트 분기(문항 유형 기반)
  * suggestion 필수 제거
* 저장 로직:

  * resources와 analysis 전체 제이슨 저장 컬럼 추가
  * manifest.json을 드라이브에 저장하고 파일 ID 저장

---

## 12) 수용 기준(Acceptance Criteria)

다음이 “통과”이면 PRD 목적 달성으로 본다.

1. 수행평가 활동지 10종에서:

   * sharedResources가 안정적으로 추출되고,
   * 문항이 resourceRefs로 공통 자료를 참조한다.
2. open_ended 문항에서:

   * 정답 단정 대신 rubric(3개 이상) + sampleResponses(2개 이상)가 나온다.
3. 이슈가 없을 때:

   * issues=[]가 가능하며, suggestion이 강제되지 않는다.
4. 이번 예시 같은 조건 카드형 수행평가에서:

   * “조건 카드 불명확” 류의 헛지적이 유의미하게 감소한다. 

---

# 당신이 남겨준 3가지 답변에 대한 “정리(전문가 없이도 이해되는 형태)”

1. **PDF를 이미지로 만들고 멀티모달로 읽는다** → 아주 맞는 방향입니다.
   여기에 제가 추가한 핵심은 “안정성”입니다.
   PDF는 레이아웃이 다양해서, **이미지 인식이 놓치는 글자**가 생길 수 있습니다. 그래서 PDF에서 뽑은 텍스트와 **서로 비교**하면 누락을 줄일 수 있습니다.

2. 스프레드시트/드라이브만 사용 → 충분합니다. 긴 텍스트도 괜찮다면, 제이슨 형태로 한 셀에 넣는 방식이 초기에는 가장 편합니다.

3. 출력 한국어 고정 + 교과 특색 반영 → PRD에 반영했습니다. 영어 과목은 원문은 영어로 남고, 설명은 한국어로 가는 정책이 가장 자연스럽습니다.

---
